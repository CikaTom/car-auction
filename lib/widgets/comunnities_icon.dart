import 'package:flutter/material.dart';

class CommunitiesIcon extends StatelessWidget {
  const CommunitiesIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.people);
  }
}
